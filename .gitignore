node_modules/
.sid
*.log
.DS_Store

# SDD scratch workspace
.superpowers/

# This install's own studio: team, start date, service label, and its math sheet.
/config.local.json
/math.json
.claude/settings.local.json

# Secrets and switches for this install (REDIS_PASSWORD, S3_BUCKET, AWS_*).
/.env
# The nightly archive when no S3_BUCKET is configured.
/stake-polling-logrotate-data/

# Agent worktrees made by Claude Code.
/.claude/worktrees/

# The demo site, built by npm run demo:build.
/dist/
